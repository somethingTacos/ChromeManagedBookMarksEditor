<Application x:Class="LinkHierarchy2JSON.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LinkHierarchy2JSON"
             xmlns:vm="clr-namespace:LinkHierarchy2JSON.ViewModel"
             xmlns:views="clr-namespace:LinkHierarchy2JSON.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Data Templates -->
        <DataTemplate DataType="{x:Type vm:ManagedBookmarksViewModel}">
            <views:ManagedBookmarksView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HelpViewModel}">
            <views:HelpView/>
        </DataTemplate>
        <!-- Styles -->
        <!-- Main Button Style -->
        <Style x:Key="MainButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="0 0 0 3"/>
        </Style>
        <!-- Main TextBox Style -->
        <Style x:Key="MainTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Application.Resources>
</Application>

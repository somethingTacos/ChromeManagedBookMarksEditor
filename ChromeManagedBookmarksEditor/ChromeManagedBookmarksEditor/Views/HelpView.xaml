<UserControl x:Class="LinkHierarchy2JSON.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LinkHierarchy2JSON.Views"
             xmlns:data="clr-namespace:LinkHierarchy2JSON.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type data:HelpTopic}">
            <Label Content="{Binding Header}"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="HeaderList"  Grid.Column="0" ItemsSource="{Binding HelpGuide.TopicCollection}"
                 SelectedItem="{Binding CurrentTopic}"/>
        <Image Grid.Column="1" Source="{Binding HelpGuide.CurrentHelpImage}"/>
    </Grid>
</UserControl>
